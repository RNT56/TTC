[package]
name = "forge-num"
description = "Deterministic transcendentals for forge-core (D17): one libm, every target"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libm = "0.2"
