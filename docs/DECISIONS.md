# DECISIONS — the binding decision record

Decisions bind all work. They are changed only by **recording a superseding decision**
(new entry, references the old one, states why) — never by silently editing docs or
drifting in code. On any conflict between documents, use the authority order in
[`/AGENTS.md`](../AGENTS.md); executable evidence still owns current-state claims.
The record below reflects plan **v3.0** §21 (D1–D18) plus subsequent append-only
architecture, policy, and execution decisions recorded here for visibility.

## How to add a decision

1. Confirm it is genuinely a decision of consequence (architecture, scope, sequencing,
   security, legal, economics, naming) — not routine implementation detail.
2. Append a row with the next free ID (next regular ID: **D74**), a one-line decision,
   a one-line rationale, and status `active`.
3. If it supersedes an earlier decision, set the old row's status to
   `superseded by Dnn` — exactly as D6 and D11 were retired by v3.0.
4. Reference the ID from the docs and code it affects; note it in your changelog entry.
5. Decisions that reverse the plan should be confirmed with the project owner first.

## Strategy decisions

| # | Decision | Rationale | Status |
|---|---|---|---|
| D1 | **Verify-first wedge**: configurator truth (component DB, P3) ships and gets marketing before generation GA (P4) | builders arrive for honest numbers, stay for the wow layers | active |
| D2 | **Open-core**: contract schema + **core crates** + `forge-validate` are Apache-2.0; platform, catalog data, compute services, marketplace proprietary. ToS states from day one that admitted models contribute anonymized structural patterns (opt-out per model; marketplace listings opt-in by default) | trust + contributions from code; the moat is the data; the Rust core makes the open half a distributable artifact (the R2 rung) | active |
| D3 | **BYO Anthropic API key + metered credits** from day one; the studio (view/configure/validate/local-sim) free forever | generation self-funding; GPU at transparent cost-plus | active |
| D4 | **Read-only share URLs at P4**, not the platform phase | sharing is the growth loop — and the standing argument for the web face | active |
| D18 | **Positioning: upstream of CAD** — mass-properties-correct over surface-exact; OCCT inside, STEP out first-class; Parasolid/CGM licensing documented as a purchasable option, never a roadmap item; non-goals stated (GD&T, surfacing, tooling, certified workflows); success ladder R1–R4 | fight where there is no incumbent, export to where there is | active |

## Runtime & technical decisions

| # | Decision | Rationale | Status |
|---|---|---|---|
| D5 | Pinned `componentRef`s + per-model **lockfile** against immutable catalog revisions; upgrades are explicit, re-validated, diffed (mass/hover/price) | catalog changes must never silently change a model | active |
| D6 | Tolerance-banded client replay with drift detection; bit-exact replay and official scorecards server-side only | obsolete: one implementation made exactness universal | **superseded by D17** |
| D7 | **Collision compounds** per node: ≤ 8 convex pieces/node, ≤ 24/model, validator-enforced; visual parts decoupled from colliders | contact fidelity where it matters; no 136-collider physics cliff | active |
| D8 | **Estimator block** in the contract; policies train on estimator output, never ground truth; scorecards reject ground-truth-trained policies | removes a top-three sim-to-real killer by schema | active |
| D9 | **Control-rate contract**: policy advises ~50 Hz; supervisor ≥ 200 Hz; FC rate loop never touched; missed inference tick degrades to fallback; numbers stated in the ladder UX | stated authority, stated degradation | active |
| D10 | **License classes** (`open / attribution / no-redistribution / view-only`) + export matrix; restricted meshes degrade to dimensioned envelopes + link-out | whole-assembly exports legal by construction | active |
| D11 | Chromium-first declared; Firefox/Safari viewer-grade; iOS viewer; bridge Chromium-only | honesty over pretense | **subsumed by D15** |
| D12 | **Reference rigs frozen at P3**: one ArduPilot-capable 5″ quad + one Pi-class differential rover; exact SKUs pinned at ingestion | de-risks P8; becomes tutorials and standing test fixtures | active |
| D12-P3 | **P3 reference rig SKUs pinned**: quad = TBS Source One V6-class 5″ frame, Holybro Kakute H7 V1.5 FC (`11091`), Holybro Tekko32 F4 4in1 50A ESC (`31102`), EMAX ECO II 2207 1900KV motors (`0101096015`), Gemfan Hurricane 51466 V2 props (`17756`), CNHL Black V2 4S 1500 pack (`1501304BK-2PACK`); rover = Waveshare UGV Rover PT PI5 ROS2 kit (`27255-PT-PI5-ROS2`) | exact local fixtures for P3 catalog/BOM/HUD; retailer-only rows remain review-gated until owner verification | active |
| D13 | **Primitive-refit acceptance**: ≥ 70 % surface-area fit coverage AND Hausdorff residual ≤ 1.5 % of bounding diagonal, else the part admits as mesh-class | "parametric" is a measured claim, not a vibe | active |
| D14 | **Failed generations persist as editable drafts** carrying diagnostics; drafts cannot train, export, or share | no vanishing work; gatekeeper stays sovereign | active |
| D15 | **Browser is the primary surface, permanently; FORGE Desktop (Tauri) ships at P8** as the bridge/power surface (serialport-rs, background recorder, real filesystem); Firefox/Safari/iOS viewer-grade by declaration | the tab wins distribution; the shell arrives when it has a real job | active |
| D16 | **Rust core, web face**: contract/geometry/motion/sim/validator as dual-target Rust crates (`forge-core`) behind a frozen DOM-free boundary (bake/tick/validate/patch); render/UI stay TS + Three.js; gateway TS; compute Python; direct JS→Rust port with the harness as parity oracle | one implementation of truth, paid for once | active |
| D17 | **One validator everywhere**: `forge-validate` as static binary + npm WASM package + crate, bit-exact across targets; no fast-math in core; cross-target **golden-number suite** in CI; platforms that break exactness degrade to declared ULP tolerance | replay verifies anywhere; the R2 rung becomes a distributable artifact | active (supersedes D6) |

## Scoping & evals

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-r1 | **Wiring v1** is cosmetic verlet splines + an exact BOM wire list; routed harness design through joints is deferred research | honest scoping of a genuinely hard problem | active |
| D-evals | **Brief-25** generation benchmark as permanent CI (re-run on every prompt/schema/pattern/model change) with tracked admission/repair/diversity metrics | generation quality is an engineering quantity with a dashboard | active |

## Structural decisions implicit in the plan (recorded here for visibility)

| # | Decision | Rationale | Status |
|---|---|---|---|
| D19 | **No code in contracts** — models are data; drivers are parameterized references into versioned engine libraries; the only future exception is the sandboxed-WASM controller (post-P7, capability-limited, no I/O, fuel-metered, marketplace-reviewed). *(Recorded as D15 in this file before v3.0 took D15–D18; renumbered.)* | the central security + generability decision (plan §4, §17) | active |
| D20 | **Two physics engines on one compiled source of truth** — Rapier (client/interactive) + MuJoCo (training/canonical), both consuming the same MJCF from the same contract, parity suite on every upgrade; training side wins disagreements. *(Previously recorded as D16 in this file; renumbered.)* | interactivity and training-grade contact have different needs (plan §6) | active |

## Project-execution decisions

| # | Decision | Rationale | Status |
|---|---|---|---|
| D21 | **Implementation started ahead of PRE-002 by owner order** (2026-06-12): the v0 end-to-end build proceeds on all surfaces while the prototype is absent. Consequences, recorded: (a) P0's byte-equivalence and the model translations stay **open** until the monolith lands; (b) the synthetic `examples/vx2-mini.forge.json` is a fixture, never a stand-in for the translations; (c) primitive parameterizations beyond Appendix A are *(proposed)* in code and reconciled at PRE-002; (d) the Rust core was implemented directly rather than ported — oracle parity (P1 golden numbers) still gates P1 close. | the owner explicitly re-ordered ("build the whole project end to end"); CLAUDE.md §10 requires this entry | active |
| D22 | **Gateway keeps binary-spawn; napi-rs deferred (OD-08 resolved by measurement, 2026-06-12).** Measured on the shipping path (temp files + execFile of the debug binary, report parsed): spawn p50 **5.3 ms** (vx2-mini, 16 parts) / **17.8 ms** (hrx7, 125 parts); in-process WASM-in-Node (a conservative napi proxy — napi would only be faster): p50 **0.7 / 3.7 ms** (`scripts/od08-measure.mjs`). Both sit far inside the < 150 ms interactive-validate budget, and spawn buys process isolation plus guaranteed bit-equality with the CI artifact. napi-rs would add a third build artifact to save ~5–15 ms — revisit only when a measured server-side hot path demands it (candidate: the P4 generation orchestrator validating every draft iteration at volume). | measurement over preference; the in-process option already exists (WASM) wherever latency matters | active |
| D23 | **The product name is ForgedTTC** (owner decision, 2026-06-12 — resolves OD-01/PRE-005). "FORGE" remains the historical working codename in frozen papers; living docs and public artifacts say ForgedTTC. **Scope call:** code namespaces stay `forge-*` / `@forge/*` — they are internal artifact prefixes, renaming them churns every crate/package/import for zero user value; public-facing naming (UI title, README, published package descriptions, NOTICE) carries ForgedTTC. A formal trademark scan remains the owner's pre-P4 action (recorded, not blocking). | owner instruction; minimal-churn scoping per "boring everywhere" | active |
| D24 | **License mechanics (implements D2, owner-delegated business calls, 2026-06-12):** Apache-2.0, copyright RNT56, scoped to the open core = `crates/` (ALL forge-* crates incl. the wasm facade and forge-gen — everything published to crates.io/npm must be usable), `schema/`, and `examples/` (fixtures are unusable if closed). Everything else (studio, gateway, workers, prototype, catalog, docs, infra, scripts) proprietary, all rights reserved. Root `LICENSE` states the split; `LICENSES/Apache-2.0.txt` canonical text; `NOTICE` per Apache convention; zone-2 package.json marked "SEE LICENSE IN". Catalog rows keep their per-row `license` metadata (D10). Contribution terms stated in LICENSE. | open core must cover every published artifact or publication (P2-001) deadlocks; examples/fixtures travel with the validator | active |
| D25 | **P4 starts with live catalog ingestion and review operations before full text-to-CAD generation GA.** The first P4 slice is the owner-facing review API/UI plus injectable fetch/Claude/OCCT adapters; the generation orchestrator then consumes reviewed catalog truth, not unaudited rows. | generation quality depends on trusted parts/provenance; closing the review loop first avoids building LLM flows on unresolved catalog debt | active |
| D26 | **P4 Anthropic model pins are fixed from official docs checked 2026-06-13:** synthesis `claude-fable-5` (1M context, 128k max output, $10/$50 per MTok input/output), repair `claude-opus-4-8` (1M, 128k, $5/$25), edit `claude-sonnet-4-6` (1M, 64k, $3/$15), ETL `claude-haiku-4-5-20251001` / alias `claude-haiku-4-5` (200k, 64k, $1/$5). Prompt-cache write/hit prices are recorded in code; `claude-mythos-5` is not used because official docs mark it limited availability. | model IDs, limits, and prices move faster than planning docs; generated artifacts must carry auditable model provenance before live calls | active |
| D27 | **P4-P12 executes as a deterministic local production slice before live cloud/hardware expansion** (2026-06-14): P4 generation uses deterministic multi-archetype templates by default, Auth.js GitHub OAuth is pulled forward, Docker Compose is the runnable stack, MinIO-compatible object storage backs blobs, and Modal remains the burst-GPU adapter behind fixture-default workers. Live Anthropic, live fetch, live photogrammetry, SB3/MuJoCo, and hardware deployment stay injectable/optional until explicitly configured. | the owner requested full P4-P12 execution, but CI/local closure must not depend on external keys, GPUs, or physical hardware | active |
| D28 | **P8 legal gate remains binding despite scaffold implementation** (2026-06-14): bridge/Desktop data tables, fixture jobs, Studio controls, replay/system-ID seams, and worker handlers may exist before counsel review; WebSerial writes, auto-deploy, free-flight/tethered steps, and any policy/hardware deployment surface remain blocked until the ToS/liability/legal sign-off record is completed. | legal gates are entry conditions, not cleanup; scaffold code must not silently turn into hardware authority | **superseded by D30** |
| D29 | **P11 marketplace launches as a usage-data beta; economics decision is deferred until real thresholds are met** (2026-06-14): listed models/courses/skills/components may collect views, equips, quote clicks, policy downloads, and training-job usage, but launch has no seller payouts, no revenue share, and no direct marketplace checkout. Credit cost-plus remains retained for GPU jobs. | pricing and revenue share need real usage distribution; quote/link handoff and usage rollups give evidence without creating a payout/payment system prematurely | active |
| D30 | **D28 hardware/legal signoff is accepted for controlled D12 lab pilots only** (2026-06-14): owner signoff covers ToS/liability posture, telemetry consent, ladder UX, physical confirmation, no-auto-arm, D12 rig allowlist, policy-advisory authority, and supervisor priority. Runtime remains lab-gated: `d28.hardware` accepted, `FORGE_HARDWARE_LAB_MODE=1`, local provider, D12 rig ID, and physical confirmation are all required. External beta is not enabled by this decision; it requires post-lab evidence and an explicit rollout gate. | unlocks the next implementation step without turning scaffolded hardware authority into a general product capability | active |
| D31 | **Package releases and persisted/public data contracts have independent SemVer domains** (2026-07-12): ModelSpec, validator reports, replay, and EnvSpec carry explicit format versions; CLI and WASM follow package SemVer; unversioned worker envelopes follow the worker package until promoted to their own schema. Additive fields are minor, semantic/type/unit removals or changes are major, and normal deprecations remain readable for at least 90 days and two minor releases after the first public replacement release. | package cadence must not silently redefine stored designs, reports, courses, or evidence; machine-readable compatibility lets every surface reject unsupported majors consistently | active |
| D32 | **The frozen pre-configurator prototype is the complete historical parity boundary; absent variants are not reconstructed or fabricated** (2026-07-13). P0-007 closes as not applicable to the delivered vintage. Product variant support proceeds independently through XC-28: a versioned contract must identify exactly one equipped variant per non-empty slot, and only that variant may affect bake, validation, simulation, BOM, lockfile resolution, or Studio state. Legacy slot-bearing 2.1 documents require an explicit deterministic migration rule before the new contract is accepted. | the only delivered oracle contains no 31-variant slot system, while the current contract incorrectly lets every alternative contribute to mass/BOM/simulation; honest provenance and physical truth require separating unavailable historical extraction from real selected-configuration semantics | active |
| D33 | **Account export and deletion are explicit primary-data operations, not implied cascades** (2026-07-13). User-data export 1.0.0 reads a repeatable snapshot of every owner-scoped dataset, exposes authenticated blob-download endpoints, and excludes OAuth/session/verification/provider secrets. Account deletion requires exact destructive confirmation, locks the owner in a serializable transaction, explicitly purges owned and derived rows, deletes all S3-compatible payloads before commit, and rolls back database deletion when object removal fails. Deletion receipt 1.0.0 says `primary` only: legal holds, retention periods, tombstones, and backup expiry/restoration remain SEC-005 and must never be inferred from a successful primary receipt. | `ON DELETE SET NULL` would orphan photos, generated artifacts, telemetry, policies, courses, and audit rows; a durable privacy boundary needs complete scope, fail-closed storage coordination, a versioned portable export, and an honest separation between primary deletion and backup lifecycle proof | active |
| D34 | **User-content consent is a versioned append-only authority ledger, scoped by purpose and owned subject** (2026-07-13). Grants and withdrawals bind consent-ledger version 1.0.0, one of five purposes, the exact owned object/account/log/model, current notice version and SHA-256, prior event, bounded non-content evidence, and an owner-scoped idempotency key. The owner row is locked and active consent is checked in the same serializable transaction as photoscan job creation, telemetry sharing, pattern contribution, leaderboard publication, or telemetry-backed training. Policy/hash drift makes an old grant inactive. Withdrawal never rewrites history: it appends an event and cancels pending affected work or removes sharing/retrieval/publication eligibility. This supersedes only D2's opt-out/marketplace-default pattern-consent mechanics: pattern contribution is now explicit per-model opt-in; D2's open-core/product boundary remains active. | a global boolean, UI checkbox, or mutable row cannot prove what was authorized, for which content, under which notice, or whether concurrent withdrawal raced an action; separate purposes prevent photoscan processing from silently becoming training, sharing, or public contribution authority | active |
| D35 | **Deletion is a primary purge plus pseudonymous restore suppression; backups are catalogued recovery copies, never an alternate live store** (2026-07-13). Data-lifecycle 1.0.0 defines versioned product retention defaults, append-only time-bounded legal holds, domain-separated subject digests, user/object deletion tombstones, a 30-day maximum backup window, a 45-day tombstone window, affirmative provider deletion with retry, and mandatory manifest/tombstone checks before restore. Account-deletion receipt 2.0.0 proves primary database/object completion and restore-suppression creation, not physical provider-backup deletion. Authority ledgers use monotonic event sequences because timestamps and random IDs cannot establish causality. Real encrypted backup automation, complete catalog coverage, provider receipts, sandbox restore, RPO/RTO, and DR promotion remain OPS-005. | primary deletion alone can be undone by an old recovery copy; indefinite flags and uncatalogued backups cannot prove storage limitation, lawful hold review, or non-resurrection. Separating deterministic lifecycle authority from production DR preserves an honest maturity boundary. | active |
| D36 | **Native P3/P4 Claude ETL is a bounded review-candidate transport, never catalog authority** (2026-07-13). The deployment command keeps precedence; otherwise the worker may call only `https://api.anthropic.com/v1/messages` with deployment `ANTHROPIC_API_KEY` in the header, API version `2023-06-01`, pinned `claude-haiku-4-5-20251001`, 8,192 output tokens, a forced strict tool, and no provider SDK dependency. Because Anthropic strict schemas support a constrained JSON-Schema subset, the tool returns a strict `canonicalRowJson`/`sourceConflicts` envelope; the worker then reparses and bounds the row locally, verifies identity, mass, confidence, license, price, and citation structure, records model/API/source provenance, and still routes it through sovereign P3 validation and human review. Official Messages, strict-tool, structured-output, versioning, and model docs were rechecked on 2026-07-13. | a provider schema cannot represent the catalog's category-dependent nested fields without either weakening shape control or using unsupported constraints; a minimal strict envelope plus stronger local validation preserves provider compatibility, evidence, and validator sovereignty without making CI or acceptance depend on secrets | active |
| D37 | **Postgres schema history is forward-only, checksum-bound, and prefix-supported** (2026-07-13). During the pre-1.0 line, every exact checked-in predecessor prefix is supported. The runner serializes with a database advisory lock, validates a contiguous source/ledger history, and commits each migration with its ledger row atomically; missing source, gaps, or checksum drift fail closed. Application rollback retains additive schema; committed defects use a new forward migration or the reviewed verified-backup recovery path. Prefix retirement requires an explicit decision and release/backup/migration guidance. | persisted authority, provenance, user data, and job state cannot tolerate concurrent deploy races, partial DDL without ledger truth, edited history, or convenience down-migrations; executable predecessor coverage makes the actual support promise reviewable | active |
| D38 | **Queued compute is at-least-once with bounded lease-fenced attempts, and client object uploads are staged until verified** (2026-07-13). Every non-fixture claim receives an opaque attempt token and expiry; only that current unexpired lease may succeed, retry, fail, charge/materialize, or emit a terminal transition. Expired work is reclaimed up to a fixed attempt ceiling, retryable timeout/rate/provider/storage faults use bounded deterministic backoff, cancellation clears the lease and always defeats late output, and terminal/exhausted work never silently requeues. Client-uploaded object rows remain `staged`; exact stored length, content type, and declared SHA-256 must be confirmed before download or photoscan consent/action, while legacy and worker-owned fixture references retain explicit `complete` compatibility status. | process death and network ambiguity make exactly-once execution unattainable; attempt fencing plus transactional materialization prevents stale/duplicate workers from publishing twice, while explicit upload completion prevents a presigned URL or partial transfer from becoming product truth | active |
| D39 | **Durable policy bytes live in one exact owner-scoped content-addressed object; persisted policy/job metadata is byte-free and one job may authorize only one policy** (2026-07-15). Canonical inline bytes are transient producer transport only. The worker checks the current D38 lease before upload and again in the serializable success/materialization transaction; that transaction binds the admitted model revision, exportable scorecard, tensor header, lineage, byte length, SHA-256, and complete object row. Studio reads through the authenticated same-origin gateway, never through worker or storage credentials, and both gateway and Studio independently verify the retained bytes before execution. Cancellation during upload may leave an unreferenced object but cannot create database authority; OPS-006 owns bounded inventory and deletion. | an upload or database row alone cannot prove that the model the browser executes is the scorecard-approved output of the winning attempt; one evidence chain and double verification prevent substitution, cross-tenant access, duplicate publication, durable inline copies, and stale/cancelled output authority without pretending distributed storage is exactly-once | active |
| D40 | **The waypoint trainer slice of P7-014 precedes P7-012; stable task IDs and the remaining rover/legged scope are preserved** (2026-07-15). Implement, test, and protect a contract-derived real waypoint environment, sequential target semantics, estimator-only tensor, task-bound scorecard/export, deterministic smoke, and unsupported-shape refusal first. Then P7-012 may run hover-hold and waypoint-chain overnight on declared consumer GPU hardware. Rover and legged trainer coverage remains in P7-014 after that evidence rather than delaying the narrower overnight gate. | the protected SB3 command currently rejects every task except `hover-hold`, while P7-012 requires real passing evidence for both hover-hold and waypoint-chain. Running P7-012 first would either fail by construction or substitute fixture metadata for executable trainer capability; dependency-complete sequencing preserves honest evidence without renumbering the ledger | active |
| D41 | **New training tasks use `p7-v2`/`2.0.0`, explicit `forge-y-up-rh-m`, and a canonical definition hash; `p7-v1` remains historical and is never silently reinterpreted** (2026-07-15). Keep stable task IDs, correct every built-in 3D task coordinate into Forge Y-up, bind new native/external policy task metadata, ONNX header, scorecard lineage, config, and browser playback to the v2 definition hash, and retain legacy single-target policy playback only as a read path. | the v1 task table stored conventional Z-up positions even though ModelSpec, Rust/WASM observers, Studio, and `forge-policy-tensor` are Y-up. Mutating v1 coordinates in place would make old evidence mean something different; a task-major correction with explicit frame/hash makes drift and substitution fail closed while preserving historical artifacts | active |
| D42 | **Executable multirotor learning advances together to policy tensor 2.0.0, training bundle 2.0.0, and training task `p7-v3`/3.0.0; every v1/v2 meaning remains immutable** (2026-07-15). Tensor v2 inserts estimator-derived body-frame linear velocity after angular rate, changing the input from `[1, 11]` to `[1, 14]`; its four outputs are normalized flight targets interpreted through a deterministic task-owned inner loop with zero collective at contract-derived hover trim. Bundle v2 adds contract-derived tilt and yaw-rate authority. Task v3 binds the corrected Forge Y-up angular-axis order, control law, reward, velocity filter, and task-completion meaning. Studio and WASM retain exact tensor-v1 read execution through version-selected observer functions, but new training and fixtures emit v2; there is no fabricated tensor migration or silent old-policy reinterpretation. | task-v2 exposed three coupled semantic defects: a memoryless position policy could not observe linear velocity, the worker treated normalized flight targets as raw torques and zero collective as half thrust, and its Z-up angular decomposition swapped Forge pitch/yaw. Fixing those meanings under v1 would corrupt historical artifacts; independent major bumps plus an executable legacy oracle preserve compatibility while making the train/browser contract physically coherent | active |
| D43 | **The P7-012 recipe runs CPU PPO on a declared GPU-capable consumer host when exact-host evidence shows MPS is slower; accelerator inventory is not an accelerator-execution claim** (2026-07-15). The frozen M2 Pro pilot compares the same 4,096-step MLP PPO workload and selects CPU after MPS measured about 13.38 s versus 1.08 s. MPS requests remain supported for diagnostics but must resolve exactly, forbid `PYTORCH_ENABLE_MPS_FALLBACK`, and record the real backend. P7-012 inventory records the 19-core MPS device while training declares CPU. Energy evidence uses a labeled adapter-rating-times-wall-time upper bound until a privileged sampler is available; it is never reported as measured consumption or converted to electricity cost without a measured tariff basis. | Stable-Baselines3's MLP PPO path is primarily CPU-oriented, and PyTorch MPS availability alone does not prove useful acceleration. Choosing the measured faster exact backend respects the overnight SLO and reproducibility while preventing a consumer-GPU requirement from becoming a false GPU-utilization, energy, or cost claim | active |
| D44 | **Rover and quadruped learning use an independent internal ground bundle/task/tensor v1; multirotor bundle v2, task v3, policy tensor v1/v2, and their browser meanings remain immutable** (2026-07-15). P7-014 admits exactly `line-follow` for rover and `walk-to-target` for quadruped. Rust derives the floating-root MJCF, explicit flat plane, mass/inertia, wheel geometry, joint limits, and every torque/velocity ceiling from the admitted contract; absent authority, unequal rover wheel geometry, unsupported morphology/task, or task/tensor substitution fails closed. The worker exposes noisy/latent estimator and joint-encoder state only, applies bounded differential-drive or per-joint torque below contract ceilings, evaluates mass/torque/friction robustness, and labels `energyWh` as simulated positive mechanical joint work rather than battery, host, electricity-cost, or field evidence. Ground ONNX may be retained behind its scorecard, but Studio rejects `forge-ground-policy-tensor` until an exact ground observer/actuator consumer is separately designed and protected. | Reusing `forge-policy-tensor` would silently turn four flight targets into drive or joint-torque outputs, while defaulting missing actuation data would fabricate physical authority. A separate internal major preserves every historical multirotor artifact, makes dynamic quadruped joint layouts explicit, and closes P7-014's controlled trainer scope without overclaiming browser playback, device transfer, or broader legged/rover task support | active |
| D45 | **Offline learning is one exact source-bound behavior-cloning warmstart followed by recipe-owned randomized PPO; only the unchanged policy scorecard can authorize export** (2026-07-15). The gateway accepts one owned `telemetryLogId` under active `training.reuse` consent, binds it to the same admitted model, and injects the immutable tape, tape SHA-256, and admitted snapshot server-side. Tape 1.0.0 must declare exact task/tensor authority, estimator-policy observations, reviewed-controller or supervisor-approved actions, and `controlled-synthetic` maturity. The current worker rejects `recorded-device`; admitting it requires a reviewed version plus P8 recorder attestation, not a client-provided label. The worker never sorts, projects, fills, or repairs the training view: it accepts 64..100,000 finite, strictly increasing exact `(observation, action)` pairs, hashes the dataset, runs the frozen `p7-offline-bc-v1` BC-to-PPO curriculum, and binds dataset, warmstart parameters, source log, ONNX, and scorecard lineage. External commands are revalidated against the exact tape and two-stage curriculum. Legacy warmstart-only output remains held. | A generic column sorter, unverified maturity label, or provider-declared warmstart can silently mix task, tensor, model, user, or ground-truth authority. Server-side source selection plus independent dataset/curriculum verification makes telemetry reuse auditable and prevents behavior cloning from bypassing D8, consent, validator sovereignty, or the existing export gate. Controlled synthetic smoke proves execution only; recorder/device/field capture, learning quality, deployed GPU operations, and transfer remain separate P8/P7-012/P7-013/EXT gates. | active |
| D46 | **Modal training is one source-bound, versioned, single-L4 function behind the D38 lease; provider retries, secrets, egress, and CPU fallback are forbidden** (2026-07-15). The gateway admits only a complete Modal environment/function-version/source/deployment-contract identity, rejects arbitrary training fields, serializes shared active/daily-credit quota checks in Postgres, and debits only after a new job row exists. Cancellation releases active capacity and may reverse the owner's product debit, but the launch remains charged to the conservative daily provider ceiling while billing is unresolved. The worker compiles the sovereign Rust bundle locally and projects only reviewed training controls plus that bundle to exact SDK 1.5.2 function `forge-workers.train_policy_gpu`; owner/model snapshots and credential-shaped extras never cross the provider boundary. The function runs Python 3.12 with exact P7 packages, one L4, one single-use container, zero provider retries, no function secrets, blocked network, restricted Modal access, and an eight-hour ceiling. The provider call ID is persisted before waiting; every ambiguous persisted result is recovery-only, never replacement authority. Recovery exhaustion retains the exact submitted call and no provider-completion timestamp for operator reattachment or termination. Cancellation revokes the D38 lease, terminates the provider container on the worker poll, discards late output, and exactly reverses one pre-materialization credit debit. Lagged provider cost gains authority only through an operator-only serializable transaction that binds the same exact call, report ID, USD amount, and reconciliation time in the job and attempt rows; exact replay is idempotent and conflict fails closed. Provider-call history, source/deployment/device evidence, and lifecycle state remain durable; recorded-device inputs are forbidden because Modal may retain call inputs/outputs for up to seven days. | A generic `gpu="any"` function, token-only “submitted” response, provider retry, unreviewed retained payload, refunded launch that silently reopens daily authority, fabricated completion, or unrecorded/ambiguously replaced call ID cannot prove which code/device ran and can leak data, duplicate output, or keep billing after product cancellation. One exact call contract plus database-fenced quota/cancel/refund/cost authority makes the seam auditable while keeping sandbox/live claims gated on real provider, billing, alert, deletion, and recovery evidence. | active |
| D47 | **The P7-010 decision uses three versioned simulation-proxy rows, concrete 12-hour CPU envelopes, and only a float64-capable JAX GPU/TPU backend** (2026-07-16). `d12-quad` binds the synthetic `vx2-mini` simulation contract to the frozen D12 quad registry identity and `d12-rover` binds the controlled training-rover contract to the frozen D12 rover registry identity; neither binding asserts an exact SKU-level hardware twin. The legged row is the controlled `qd-mini` reference and is not a D12 rig. CPU overnight means one exact scorecard-passing recipe within 43,200 seconds; tier 2 means the complete declared 200-candidate workload within 43,200 seconds. Decision request/result 2.0.0 binds those raw budget-artifact hashes, a current retained CPU/accelerator USD/hour source, all three contract/authority/MJCF hashes, source/runtime/hardware, and the frozen parity protocol. JAX must resolve exactly to requested `gpu` or `tpu`, float64 stays mandatory, and fallback is forbidden. Apple Metal is not accepted because Apple's current primary documentation labels the plug-in experimental and lists `np.float64` as unsupported; changing precision/backend semantics requires a superseding decision and compatibility major. Complete evidence rejects MJX when CPU meets both budgets; it adopts only when CPU needs help and every morphology preserves parity and at least 3x cost-normalized throughput. | The prior single CPU-backed hover row proves the harness but cannot answer the product decision. Explicit proxy labels prevent a registry hash from fabricating physical twin fidelity; fixed envelopes make “overnight” and tier-2 saturation testable; exact device and cost authority prevent available hardware or a marketing price from becoming accelerator/economic evidence; preserving float64 keeps one parity meaning across native MuJoCo and MJX. | active |
| D48 | **Desktop serial authority begins with one versioned, digest-bound, non-arming Betaflight configuration artifact** (2026-07-16). `forge-bridge-config/1.0.0` targets only the D12 reference quad and the reviewed Betaflight 2025.12 CLI contract. Its complete command body is exactly `set failsafe_delay = N` plus `save`, where `N` is an integer from 2 through 200 deciseconds; the current official [CLI reference](https://betaflight.com/docs/wiki/guides/current/Cli) permits 1–200 while the official [failsafe guidance](https://betaflight.com/docs/wiki/guides/current/Failsafe) identifies 200 ms as the minimum safe guard time. The producer and native consumer independently enforce schema, firmware/version, exact command set/order, safety and physical-confirmation flags, and SHA-256 over the canonical line array. Native transmission additionally requires the hardware-enable, D30-signoff, and lab-mode env gates, the D12 quad ID, exact confirmation text, 115200 baud, and an OS-enumerated serial port. The serialport-rs receipt proves only exact bytes transmitted: `targetFirmwareVersionVerified=false`, `applicationVerified=false`, and operator readback is mandatory. No auto-arm, mixer, mode, rate-loop, or arbitrary CLI authority exists in v1. | A syntactically safe string is not sufficient hardware authority. Narrowing the first writable artifact to one sourced failsafe parameter makes the cross-language boundary auditable and prevents command, firmware, rig, path, or hash substitution. Honest receipts keep a successful write/flush from becoming a false applied-configuration or hardware-safety claim; target handshake/readback, real FC evidence, HITL, tethered operation, and field proof remain separate P8 tasks. | active |
| D49 | **A successful native configuration receipt requires a bounded two-session Betaflight handshake, persistent-save acknowledgement, and exact reboot readback** (2026-07-16). Keep `forge-bridge-config/1.0.0` byte-for-byte unchanged. The Desktop first requires the props-removed confirmation, enters the CLI, and accepts exactly one stable numeric `2025.12.x` identity line with MSP API authority from `version`; only then may it send the D48 artifact. It requires exactly one `failsafe_delay set to N` acknowledgement and `# saving`, waits two seconds, polls the same OS-enumerated path for at most fifteen seconds, reopens it, requires the same reported firmware-identity hash, and accepts exactly one matching `get failsafe_delay` value. Each CLI response is UTF-8/control-byte checked, capped at 16 KiB, and bounded to three seconds. Any malformed, ambiguous, wrong-version, timeout, reconnect, identity-drift, error, or readback mismatch returns no success receipt; errors after transmission explicitly say the rig may be partially configured and must remain disarmed for manual inspection. Only the complete path emits `forge-bridge-serial-receipt/2.0.0` with target-version/application verification true, the exact reported full patch version and identity hashes, SHA-256 digests for the exact pre-version, set/save, post-version, and readback response bytes, normalized readback-line value/hash, operator-readback false, and CLI arming still disabled. The official current [CLI reference](https://betaflight.com/docs/wiki/guides/current/Cli), stable [2025.12.5 release](https://github.com/betaflight/betaflight/releases/tag/2025.12.5), and tagged [`version`/`get`/`save` implementation](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/cli/cli.c) were rechecked on 2026-07-16. | A flushed byte count cannot distinguish a wrong firmware, rejected setting, reboot failure, different reported build, or unapplied value. The major receipt bump prevents old transport-only evidence from being silently reinterpreted as application proof, while bounded parsing and a second session make every success claim executable. The reported firmware line is not a device-unique identifier, and pseudo-terminal proof is not a real FC, lab, HITL, tethered, supervisor, or field result; those remain P8-001/P8-009/P8-014/EXT-004. | active |
| D50 | **Desktop recording uses one in-shell background thread and a versioned, private-by-default, clean-stop replay archive; local serial capture never self-attests device provenance** (2026-07-16). `forge-recorder-archive/1.0.0` accepts only OS-enumerated 115200-baud D12 lab serial input after D30 env gates and the exact telemetry-consent phrase. The dedicated wire codec is contiguous, strictly time-increasing `forge-telemetry-frame/1.0.0` JSONL bound to one artifact ID; every frame is finite, object-shaped, depth/node/64-KiB bounded, and the complete archive is capped at one million frames and 512 MiB including replay/index plus reserved metadata. The recorder runs independently of the webview UI in one exclusive in-process thread, retains canonical `telemetry.frames.jsonl`, a sparse byte-offset index, and finalizes replay 1.0.0 plus `forge-recorder-receipt/1.0.0` only after an explicit stop drains buffered input, flushes and syncs both append-only files, rejects partial/empty/drifted input, and hashes frames, index, and replay. Existing paths are never overwritten. The manifest/replay/receipt bind contract, lockfile, environment, seed, rig, hashed source port, no-auto-arm, exact capture-consent confirmation, user ownership, sharing false, training reuse false, `local-serial-integration`, and `recordedDeviceAttested=false`; capture consent grants neither sharing nor training reuse. Interrupted or invalid archives retain no success receipt or completed replay; pseudo-terminal proof closes only deterministic Desktop capture mechanics. A real adapter/device identity, OS power/suspend behavior, external sharing consent, `training.reuse`, recorded-device attestation, field session, ghost, and system-ID acceptance remain separately gated. | A window-owned callback, mutable JSON array, or caller-provided maturity label cannot survive UI lifecycle, scale to indexed logs, distinguish complete evidence from a crash, or authorize offline learning. An in-shell thread is the smallest cross-platform background boundary that keeps capture alive while the webview is inactive without adding a sidecar supply-chain surface. Separate raw frames, index, replay, and receipt make interruption and tampering visible; explicit false privacy/provenance flags prevent a successful local pseudo-terminal capture from becoming a device, sharing, training, or field claim. | active |
| D51 | **Desktop archive import is a read-only, streaming v1 verifier that reports local self-consistency without promoting provenance or consent** (2026-07-16). The importer accepts exactly the five canonical `forge-recorder-archive/1.0.0` files in one real directory: no symlink, special, missing, or extra entry is readable authority, and actual aggregate bytes remain under 512 MiB. Manifest and receipt must be strict canonical JSON with their exact v1 versions, filenames, D12 rig, source/hash/environment bounds, completion, private/no-training/no-auto-arm flags, and false device attestation. The verifier streams canonical frames and index entries without loading the tape or replay into memory; it rechecks finite increasing time, bounded object state, frame count/duration, exact index stride/final entry/sequence/time/byte offsets, frame/index hashes, and a byte-exact reconstructed replay-v1 hash against both the retained replay and receipt. `inspect_recorder_archive` is read-only and does not require hardware-enable authority. Studio calls it through exact `@tauri-apps/api` 2.11.1, validates the versioned `forge-recorder-inspection/1.0.0` response and every false authority flag, and displays only bounded paths/identity/hashes/counts/nonclaims; no frames are uploaded or materialized into gateway telemetry. A passing inspection means the local files are mutually consistent under archive v1. It is not a signature, independent authenticity proof, unique device identity, recorded-device attestation, field session, sharing grant, training-reuse grant, ghost/system-ID result, or lab evidence. | Importing a replay by parsing one JSON file or trusting its receipt would miss path substitution, alternate encodings, sparse-index drift, rewritten privacy flags, and replay/frame divergence; loading a 512-MiB archive into Studio would also create an avoidable memory and UI-lifecycle boundary. One native streaming verifier preserves D50's compatibility meaning, keeps filesystem authority in Desktop, and makes the first Studio import surface useful without turning self-consistent user-controlled bytes into external provenance. | active |
| D52 | **Desktop recorder control is one strict versioned shell-state boundary; Studio supplies capture intent only from an admitted report and never receives raw frames** (2026-07-16). `forge-recorder-control/1.0.0` is an ephemeral command-response contract with exact `inactive`, `recording`, and `finished` states. The native shell owns the active recorder across webview reloads and returns bounded capture identity, archive paths, D12 rig, contract/lockfile/source-port hashes, rate, start time, and explicit privacy/provenance nonclaims; inactive state carries no capture identity, while finished state retains identity until explicit stop collects the clean receipt or fail-closed recorder error. Studio strictly parses bridge, OS-enumerated port, control-status, and persisted receipt responses. Start is unavailable in a browser or without the D30/D12 native gates, exact per-log capture consent, one enumerated port at 115200 baud, a new absolute archive path, and an active admitted validator report whose contract hash, lockfile hash, and seed are passed unchanged. Stop remains available when a running recorder must be drained even if the hardware-enable environment later changes. Successful stop returns the existing `forge-recorder-receipt/1.0.0`; archive/frame/replay/receipt v1 bytes and meanings do not change. Control state always keeps device identity, recorded-device attestation, field verification, sharing, and training reuse false and exposes no upload or gateway materialization path. | A React-owned recording flag would be lost on reload and could allow concurrent starts, while accepting user-entered hashes or a caller-declared maturity would detach capture from admitted evidence. A separate ephemeral control schema makes UI/native drift fail closed without pretending a start acknowledgement is a persisted archive, and shell-owned status lets operators recover or collect failure after the UI lifecycle changes. Keeping stop and receipt semantics separate preserves D50/D51 compatibility and leaves authenticity, object-backed materialization, device attestation, host suspend, lab, and field proof to their owning gates. | active |
| D53 | **Recorder object materialization is an exact five-object, native-streamed, owner-private boundary; object integrity is permanently separate from archive semantics and provenance** (2026-07-16). Desktop reruns D51 and emits path-free `forge-recorder-upload-plan/1.0.0` with exact identity, authority nonclaims, and five canonical file name/type/size/SHA-256 declarations. The authenticated gateway stages five distinct private content-addressed objects and migration 0025 retains `forge-recorder-materialization/1.0.0`. Native Desktop alone reads local files and streams sized PUT bodies to one explicitly configured HTTPS origin (loopback HTTP only), requiring complete signed query fields and exact content-type/checksum headers while disabling redirects and reqwest's implicit system proxy. `forge-recorder-upload/1.0.0` proves only native upload completion and leaves gateway integrity false. Gateway completion independently HEAD-verifies every object and reads only bounded manifest/receipt JSON to bind artifact, rig, contract/lockfile/source hashes, times/count, and frame/index/replay object digests. Only then may `gatewayObjectIntegrityVerified` become true; `gatewayArchiveSemanticsVerified`, recorded-device/device/field authority, sharing, and training reuse remain false by code and database constraint. No row is inserted into `telemetry_logs`, and archive v1 bytes/meanings do not change. | Sending a 512-MiB tape through React/Fastify JSON or JSONB would violate memory and request limits, while trusting native upload success or HEAD metadata would turn transport into semantic/provenance authority. A path-free plan plus native same-origin streaming preserves filesystem and memory boundaries; five independent checksums make partial/substituted objects visible; bounded metadata cross-binding establishes useful object integrity without pretending the gateway replayed every frame/index byte or authenticated its physical source. Sovereign server streaming archive admission, reviewed adapter/device identity, production object operations, suspend, lab, and field evidence remain separate gates. | active |
| D54 | **Sovereign recorder admission is a separate native-verifier proof and bounded object-backed telemetry reference; D53 remains immutable and recorded-device/training authority stays false** (2026-07-16). The explicit authenticated action accepts one owner-selected model ID only after D53 materialization. The gateway streams the exact five private objects with length/SHA-256 enforcement into a mode-0700 temporary root and mode-0600 exclusive files, invokes native `forge-validate recorder-verify`, and removes the complete temporary root before persistence. The validator independently enforces canonical archive/frame/index/replay/receipt v1 semantics, aggregate/frame/node limits, exact sparse offsets/count/duration, and reconstructed plus retained hashes, then emits `forge-recorder-verification/1.0.0`. The gateway exact-binds that report to the D53 plan/objects and the selected owned admitted model's contract/lockfile proof. Migration 0026 stores a separate `forge-recorder-admission/1.0.0` row and one small `forge-recorder-telemetry-reference/1.0.0` descriptor in `telemetry_logs`; replay frames never enter request JSON or JSONB. D53's `gatewayArchiveSemanticsVerified` remains false, while admission semantics alone become true. Database, runtime, and Studio keep recorded-device/device/field/sharing/training authority false and no-auto-arm true. D45 explicitly rejects the object-backed descriptor even if a later training-consent grant exists. | Reinterpreting the D53 row would violate its permanent object-integrity-only meaning, while a TypeScript replay parser could drift from Rust canonical number/byte semantics and a full JSONB tape would violate the 512-MiB memory boundary. A separate native proof preserves validator sovereignty and compatibility; bounded temporary streaming avoids large heap/RPC payloads; an explicit model choice prevents hash guessing; and a reference row makes export/deletion and future readers auditable without converting self-consistency into physical provenance. Reviewed adapter/device/session identity, production object operations, suspend, lab/field, ghost/system-ID, and any recorded-device training format remain separate versioned gates. | active |
| D55 | **The first real-adapter prerequisite is a read-only, two-pass Betaflight MSP identity observation; self-reported UID/board bytes are never cryptographic attestation or archive provenance** (2026-07-16). Ephemeral `forge-recorder-adapter-probe/1.0.0` binds adapter `forge-betaflight-msp-adapter/1.0.0` to the D30-gated, props-off D12 reference quad on one OS-enumerated 115200-baud port while the native recorder is atomically held inactive. Native Desktop sends only exact MSP-v1 read requests `[MSP_API_VERSION, MSP_FC_VARIANT, MSP_FC_VERSION, MSP_BOARD_INFO, MSP_BUILD_INFO, MSP_UID]` (`[1,2,3,4,5,160]`), bounds every response to the one-byte MSP-v1 payload limit and three-second deadline, and rejects direction, command, checksum, protocol/API, firmware, target, board-tail/build-option shape, placeholder-UID, or pre/post drift. Both complete passes must report protocol 0, API 1.47, `BTFL`, one stable `2025.12.x`, and `KAKUTEH7`. The response contains only bounded decoded board labels plus domain-separated hashes of the source port, OS descriptor, 96-bit UID, normalized identity, each exact response set, and the ordered twelve-response transcript; raw UID and raw response bytes never cross into Studio. The result is fixed to `unattested-read-only-probe`, adapter/stability true, and device-identity verification, cryptographic attestation, recorded-device, field, sharing, and training authority false with no-auto-arm true. It is not persisted into archive v1 or D54. The official current [MSP reference](https://betaflight.com/docs/development/MSP-Protocol-Reference-Dev) and tagged 2025.12.5 [`msp_protocol.h`](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/msp/msp_protocol.h), [`msp_serial.c`](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/msp/msp_serial.c), [`msp.c`](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/msp/msp.c), [`msp_build_info.c`](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/msp/msp_build_info.c), and [`version.h`](https://github.com/betaflight/betaflight/blob/2025.12.5/src/main/build/version.h) were rechecked on 2026-07-16. | A port name or CLI version line is not device identity, while MSP UID and board replies can be replayed or emulated and carry no trusted signature. A narrow native read-only probe makes the protocol, privacy, stable-session, and nonclaim boundary executable without inventing authenticity. Real named-FC capture must later bind this exact identity at recorder start/end plus retained custody/lab evidence; any cryptographic or operator attestation, archive-v2 provenance, D54 promotion, or recorded-device training format requires a separate reviewed decision and compatibility major. | active |
| D56 | **Recorder custody is a separately signed, expiring authorization plus native start/end identity-continuity proof; it never rewrites archive v1, D53, or D54** (2026-07-16). A future `forge-recorder-custody-authorization/1.0.0` must carry one canonical binding signed under an exact purpose-limited Ed25519 trust root configured by deployment. The hash-pinned `forge-recorder-custody-trust-bundle/1.0.0` contains public keys only, at most 32 bounded key IDs, validity/revocation metadata, and the `controlled-lab-recorder-custody` purpose; Desktop never receives a signing key. The authorization binds one opaque authorization ID, full protected product revision, `forge.external-acceptance.v1` evidence-pack hash, required-role/signoff-set hash, D30/D12 controlled-lab scope, exact artifact/contract/lockfile, telemetry and identity-port OS-descriptor hashes, D55 probe/adapter versions, expected normalized identity and UID hashes, an eight-hour-or-shorter validity window, no-auto-arm, and permanently false sharing/training/field/recorded-device/device-cryptographic-attestation authority. Desktop must use strict [RFC 8032](https://www.rfc-editor.org/rfc/rfc8032) Ed25519 verification over a domain-separated canonical binding, reject weak/malformed keys, signatures, unknown/revoked/wrong-purpose roots, clock/binding/revision/port drift, and keep raw signature material outside Studio. Start re-enumerates both signed ports, reruns the exact D55 read-only observation before opening the telemetry stream, and stores the verified authorization plus pre-observation in shell-owned state. Clean stop first preserves the existing v1 receipt, then requires a new props-off confirmation, re-enumerates and reruns D55, exact-matches the signed and pre-observed identity/UID/descriptor hashes, and writes a create-new `forge-recorder-custody-proof/1.0.0` outside the exact five-file archive. The proof binds the authorization/trust-bundle/evidence hashes, pre/post transcript and response hashes, canonical v1 receipt hash, artifact/model inputs, and explicit nonclaims. A recorder or post-probe failure creates no custody proof; a post-probe failure does not erase or relabel an already valid v1 archive. Fixture keys/probes prove only contract mechanics. A real trust bundle, semantically reviewed retained acceptance pack, named Kakute H7 V1.5, host-suspend evidence, and EXT-004 signoffs are still required before sandbox/live/field or recorded-device claims. Any gateway materialization, D54 promotion, recorded-device training format, or archive-v2 provenance is a later decision and compatibility major. | D55 can be replayed, while placing caller-authored identity fields into the manifest would turn self-report into provenance and break every protected v1 boundary. A short-lived signed authorization makes the reviewer-controlled mapping between named evidence, identity port, and telemetry port explicit; native bracketing makes substitution during one capture visible; and a separate create-new proof lets custody fail closed without corrupting the user's otherwise valid archive. Purpose-limited public trust roots, exact time/revision/port bindings, and strict verification constrain replay and key misuse, while permanent nonclaims distinguish a signature by the acceptance authority from a signature by the physical device. | active |
| D57 | **Ghost replay v1 is a compact, indexed visualization artifact with permanent provenance nonclaims; raw recorder frames remain object-backed and outside job JSON/JSONB** (2026-07-16). Internal `forge-ghost-overlay/1.0.0` binds `forge-y-up-rh-m`, exact compact rows `[timeS, actualXM, actualYM, actualZM, predictedXM, predictedYM, predictedZM, divergenceM]`, finite strictly increasing time, Euclidean meter divergence, at most 600 seconds/100,000 source samples/6,001 render points, deterministic uniform decimation with exact endpoints, and a monotonic at-most-one-second sparse seek index. The worker refuses duplicate/non-finite/out-of-range time and unsupported metrics; incomplete actual/predicted pairs disable geometry instead of fabricating it. The current worker path labels input `unverified`; the gateway's keyless ten-minute fixture alone may label its generated trace `controlled-synthetic`. Both permanently set device identity, recorded-device, and field verification false. Studio independently rejects version/layout/index/value/divergence/authority drift, precomputes top-down X/Z polylines, and uses the sparse index plus interpolation for explicit play and ±1/60-second steps. The 60 Hz unit budget covers indexed seek computation; full browser/render proof requires the production-browser interaction, while real-mid-hardware and real Desktop-log proof remain P1-018/P8-014/EXT-008. D50-D56 bytes and meanings do not change. A future real path must stream one owned D54 replay plus one exact admitted twin through a separately reviewed server-side job/object boundary; client fields or a maintenance-record insert may never supply telemetry, twin, device, or field authority. Changing row order/units/frame, decimation/index meaning, limits, divergence semantics, or provenance flags is a major-version change. | The existing crash scrubber moved a slider over summary metadata and could neither show measured-versus-predicted geometry nor substantiate ten-minute seeking. Persisting raw recorder frames in maintenance JSONB would violate D53/D54's bounded object boundary, while trusting caller labels would turn a visualization into provenance. One small decimated artifact makes the interaction and performance mechanics testable now, preserves exact recorder custody semantics, and leaves real twin execution and field evidence with their owning gates. | active |
| D58 | **The deployment ladder begins as a shell-owned rehearsal-only state machine; completing its UX stages grants no hardware, physical-evidence, deployment, device, field, or external-beta authority** (2026-07-16). Internal `forge-deployment-ladder/1.0.0` and ephemeral Tauri `forge-deployment-ladder-control/1.0.0` freeze one D12-scoped `inactive|rehearsing|rehearsal-complete` session, the exact SITL → HITL → constrained → free prefix, D9's 50 Hz advisory/200 Hz supervisor/FC-loop-untouched/fallback meanings, and exact start, HITL, constrained, free, and end interactions. Studio may start only from its active admitted report hashes, an exportable policy artifact for that model, and an exact passing supervisor-decision shape; native code independently bounds the request, owns progress across webview reloads, refuses parallel sessions, skips, substituted IDs/hashes/rates/phrases, and performs no hardware I/O. Every status permanently keeps policy advisory, supervisor authority, and no-auto-arm true while `deploymentEvidenceVerified`, `physicalConfirmationEvidenceVerified`, `hardwareExecutionAuthorized`, `deviceIdentityVerified`, `fieldSessionVerified`, and `externalBetaEnabled` remain false. Browser builds show the ladder and its nonclaims but cannot transition it. A real HITL or later transition requires a separately reviewed hardware/evidence boundary that consumes named D12 device, D30 authority, supervisor/kill-switch timing, and retained physical confirmation; it may not reinterpret v1 client acknowledgments as evidence. Changing stage order, rate/fallback authority, confirmation meaning, state/progress semantics, or any authority bit requires a major and superseding decision. | A React stepper or checked box can demonstrate interaction and skip prevention, but it cannot prove that hardware was connected, restrained, supervised, or operated. Keeping progress in the native shell makes reload/parallel-session behavior deterministic; permanent nonclaims let the product rehearse the complete flow now without weakening D30, supervisor sovereignty, no-auto-arm, lab acceptance, or field gates. | active |
| D59 | **Engine-backed co-design begins as an exact-snapshot controlled smoke, not as the overnight optimizer or a trained-finalist claim** (2026-07-16). Internal `forge-codesign-evaluation/1.0.0`, `forge-codesign-native-evaluation/1.0.0`, and `p9-engine-smoke-evidence/1.0.0` bind one Gateway-owned admitted snapshot, every replace-only JSON Patch and recomputed candidate SHA-256, the native validator report, one deterministic second of Rapier 0.33.0 at 120 × 1/120 s with two substeps, an exact Rust-derived training bundle 2.0.0, and two 200-step estimator-controller rollouts under pinned MuJoCo 3.9.0. The reviewed `FORGE_CODESIGN_CMD` accepts only 3–9 inline-multirotor candidates from a bounded motor-Kv/prop-diameter/battery-capacity scale grid; client manifolds, arbitrary external output, hash/proof substitution, and 200-candidate requests fail closed. Tier-0's <50 ms release-binary SLO is measured and reported separately from physical candidate admission so host load cannot change a design verdict. Tier 3 is always held; trained policy, CMA-ES, Optuna TPE, catalog-choice search, overnight-200 completion, provider sandbox, build readiness, hardware authority, and field evidence remain false. The additive `forge-validate codesign-evaluate` command is part of the pre-release 0.2.0 CLI line. Changing patch/hash preimages, native/Rapier protocol, controller/task/rollout meaning, tier admission, or nonclaims requires a new internal major and superseding decision. | The previous external seam returned provider-shaped dictionaries almost unexamined, while its four tier records and runtimes were synthetic. One small repository-owned engine path makes validator sovereignty and real Rapier/MuJoCo evidence executable now, without relabeling a three-candidate electrical smoke as the P9 overnight morphology-and-policy objective. | active |
| D60 | **Real CMA-ES and Optuna TPE first produce a deterministic, exact-snapshot 200-proposal plan; proposals have no physical, engine, admission, Pareto, overnight-result, or downstream authority** (2026-07-16). Internal `forge-codesign-search-plan/1.0.0` and `p9-search-plan-evidence/1.0.0` bind one Gateway-owned admitted inline-multirotor snapshot, exact source/dependency-manifest lineage, seed, constraints, frozen manifold, 100 `cmaes==0.13.0` continuous proposals and 100 `optuna==4.9.0` TPE mixed categorical/continuous proposals, every replace-only patch SHA-256, every recomputed candidate-snapshot SHA-256, and deterministic replay. The categorical variable is an explicit electrical proposal profile; continuous variables are bounded motor-Kv, prop-diameter, and battery-capacity scales. Both algorithms receive only `bounded-diversity-acquisition-v1`, whose physical-objective and engine-feedback fields are false. The plan never enters candidate evaluation and permanently keeps validator, Rapier, MuJoCo, physical-constraint, admission, Pareto, overnight-200-candidate, trained-finalist, catalog-choice, provider-sandbox, build, hardware, and field claims false. Caller manifolds and any budget other than exactly 200 fail closed. The current pins were checked against the official [Optuna 4.9.0](https://pypi.org/project/optuna/) and [`cmaes` 0.13.0](https://pypi.org/project/cmaes/) package records on 2026-07-16. A later separately versioned engine-consumption artifact must accept these exact proposal hashes, attach D59-equivalent sovereign evidence per candidate, retain recovery/cost/scheduling proof, and independently derive admission and Pareto; it may not promote this proposal plan. Changing the seed/manifold/patch/hash preimages, proposal allocation or algorithm/acquisition semantics, dependency pins, replay meaning, or any held nonclaim requires a new internal major and superseding decision. | Running the requested libraries is meaningful evidence only if their output remains distinguishable from physical optimization. Separating deterministic search planning from expensive sovereign evaluation makes breadth, lineage, and replay executable without turning a synthetic acquisition score or fast local command into a 200-candidate engine, overnight, trained-finalist, provider, build, or field result. | active |
| D61 | **The 200-candidate engine consumer is a separately versioned, per-candidate checkpointed exact-hash batch; local completion is not overnight/provider proof and tier 3 remains held** (2026-07-17). Internal `forge-codesign-engine-batch/1.0.0` accepts only the exact admitted snapshot plus a deterministically replayed D60 plan with exactly 200 proposals. Candidate ordinals form one contiguous append-only prefix; before authority is retained, the worker reapplies the exact replace-only patch, recomputes patch/candidate hashes, invokes the D59 sovereign native validator and eligible Rapier 0.33.0/MuJoCo 3.9.0 ladder, and binds native-evaluation/training-bundle/engine hashes. The same hash-bound artifact is durably replaced after every candidate. An unfinished last attempt becomes `interrupted` before resume; cancellation is recorded only when it dispatches zero new candidates. Partial/cancelled artifacts expose no Pareto or finalists. Only a complete 200-row artifact independently derives admission and Pareto, then selects up to three real engine-admitted Pareto candidates as `tier3Status=held-not-run`. Measured local candidate/attempt wall runtime is recorded with provider amount/currency/billing and energy measurement explicitly absent. The clean-source evidence wrapper is `p9-engine-batch-evidence/1.0.0`. `overnight200Candidate`, trained-finalist, catalog-choice, provider-sandbox/billing, build, hardware, and field claims remain false. Changing plan-consumption, prefix/checkpoint/hash preimages, attempt/cancellation/recovery semantics, engine/task/rollout meaning, admission/Pareto/finalist selection, cost fields, or a held nonclaim requires a new internal major and superseding decision. | A 200-proposal list is not an evaluated optimizer, while one monolithic process would lose expensive progress and make retry/cancellation authority ambiguous. A contiguous durable prefix makes recovery and duplicate avoidance inspectable, and recomputing the front only at completion prevents partial results from masquerading as a finished search. Keeping local runtime separate from overnight/provider billing and selecting but not training finalists closes the next dependency without fabricating scheduling, cost, tier-3, catalog, build, hardware, or field maturity. | active |
| D62 | **Cross-platform co-design plan identity is a blocking reproducibility gate before tier-3 or heterogeneous scheduling; D60/D61 v1 remain fail-closed and platform-scoped** (2026-07-17). Downloaded protected D61 artifact `8396554544` at Linux x86-64 source `1c37567` repeats the PR runner's exact 200 patch/candidate hashes and returns 87 admissions, 123 eligible Rapier/MuJoCo rows, four Pareto points, and the same three finalists. The clean local Apple-arm64 run at implementation head `6c446a5` returns 89/125 and the same final front/finalists, but candidate hashes differ at CMA-ES ordinals 20–99; ordinals 0–19 and all Optuna TPE ordinals 100–199 match. Existing exact-plan replay therefore prevents silent cross-platform resume by rejecting the foreign plan, but v1 does not prove portable deterministic generation and records no platform identity. Until a v2 design is protected, one D60/D61 batch must stay on one pinned architecture/runtime and may not feed tier 3, overnight/provider scheduling, or a portable cache claim. D62 must either make all 200 proposal hashes cross-platform identical through reviewed canonical arithmetic/update semantics or add an exact platform/runtime authority and explicit same-platform scheduling/refusal; any replay/hash/cache meaning change requires coordinated D60/D61 internal majors and compatibility evidence. Protected D63 at `54385c2` satisfies the explicit-authority branch; v1 remains historical/platform-scoped and cross-runtime cache, resume, and tier-3 authority remain false. | The exact protected artifact exposed a real boundary that same-host replay tests could not see. Treating stable finalists as sufficient would hide 80 changed candidate preimages and make recovery availability depend on where a worker lands. Fail-closed v1 preserves evidence integrity, while a dedicated parity lane prevents platform-sensitive optimizer state from becoming unrecorded nondeterminism or trained-policy authority. | active |
| D63 | **D62 closes through exact numeric-runtime authority and same-runtime refusal, not by claiming cross-platform CMA-ES identity** (2026-07-17). Coordinated internal `forge-codesign-search-plan/2.0.0`, `p9-search-plan-evidence/2.0.0`, `forge-codesign-engine-batch/2.0.0`, and `p9-engine-batch-evidence/2.0.0` bind `forge-codesign-proposal-runtime-authority/1.0.0`. That authority hashes OS system/release/version/machine/libc/byte order, CPython implementation/version/cache tag, the NumPy 2.5.1 distribution RECORD, build configuration, CPU feature map, BLAS/LAPACK metadata, and the `cmaes` 0.13.0/Optuna 4.9.0 distribution RECORDs. Plan and batch cache keys are partitioned by the authority hash. Plan source and batch scheduler set `resumePolicy=exact-proposal-runtime-authority` and `heterogeneousResumeAllowed=false`; every engine candidate binds the same authority hash, and validation rejects a well-formed foreign-runtime plan before proposal evaluation or checkpoint resume. `p9-platform-authority-comparison/1.0.0` compares all 200 patch/candidate hashes from two clean artifacts at one source revision while retaining false cross-runtime cache/tier-3 authority. PR #113/protected `54385c2`, artifact `8397860593`, and comparison `d255c441…6562` protect this branch and confirm 120 matching plus 80 differing CMA-ES lineages at ordinals 20–99 across clean same-source Linux/Apple plans. V1 artifacts remain historical evidence and are not silently reinterpreted. Changing authority fields/hash preimages, cache partitioning, foreign-runtime refusal, or plan/checkpoint runtime binding requires coordinated new majors and a superseding decision. | A custom floating-point eigensolver or ad hoc rounding could make one observed pair match while weakening real CMA-ES semantics and leaving other CPUs/BLAS builds unproven. Exact runtime authority preserves the pinned libraries, makes availability constraints inspectable, partitions caches safely, and fails closed until a deliberately scheduled compatible worker is available. Cross-platform equality can still be pursued later as a separately versioned improvement, but it is not needed to preserve evidence integrity or resume correctness. | active |
| D64 | **Catalog-backed co-design searches exact equipped revisions under byte-level catalog authority; synthetic profile semantics remain historical v2 only** (2026-07-17). Coordinated internal `forge-codesign-search-plan/3.0.0`, `p9-search-plan-evidence/3.0.0`, `forge-codesign-engine-batch/3.0.0`, and `p9-engine-batch-evidence/3.0.0` replace the four D60 electrical preference labels with the two exact lockfile-pinned CNHL 1500/1300 battery variants in the admitted proof contract. `forge-codesign-catalog-choice-authority/1.0.0` hashes every sorted `catalog/components/*.json` filename plus its raw-row SHA-256, then binds choice/slot/variant/ref/exact revision, row hash, mass, capacity, discharge, confidence, review state, and license/export source. CMA-ES searches continuous driver limits on the base equipped choice; Optuna TPE searches both the exact categorical revision and those limits. Every replace-only patch changes the sole D32 `equippedVariantId`, matching inline capacity/discharge mirror, and driver bounds. `forge-codesign-native-evaluation/2.0.0` plus `forge-codesign-catalog-proof/1.0.0` re-run sovereign admission with `FileCatalog` and record only equipped rows; v1 remains the no-catalog historical artifact. Batch cache, checkpoint, resume, candidate, and native proof bind both catalog and D63 numeric-runtime authorities; foreign catalog bytes fail before evaluation/resume. Catalog rows remain review-required, marketplace-publication-reviewed/exposable false, and no live catalog persistence is claimed. Training-bundle 2.0.0 is admitted with the same catalog but deliberately retains its inline-mirror powertrain semantics; this decision does not claim catalog-native MuJoCo mass/inertia/thrust-table physics. Tier 3, retained overnight/provider scheduling/billing, build, hardware, field, and external acceptance remain separate. Changing choice fields/hash preimages, equipped materialization, catalog/native proof meaning, mirror semantics, review/license authority, or a held nonclaim requires new internal majors and a superseding decision. | A categorical label cannot prove which purchasable component was evaluated, and evaluating catalog refs through `EmptyCatalog` makes an optimizer contradict normal admission. Exact revision and raw-row authority preserve D5/D10/D32 provenance while keeping marketplace review and downstream simulation maturity honest; retaining explicit inline-mirror limits prevents this lane from turning an admission fix into a false claim of fully catalog-native training physics. | active |
| D65 | **Catalog-bound multirotor training uses a separate catalog-physics bundle major and applicability-gates every bench table; inline v2 and ground v1 remain immutable** (2026-07-17). `forge-validate training-bundle --catalog` emits `trainingMuJoCoBundle` 3.0.0 plus `forge-training-catalog-physics/1.0.0` only after sovereign catalog admission. V3 binds the complete file-catalog authority, equipped slot/variant/ref/exact revision/raw-row hashes, review/license source, quantities, and mount nodes. Its total mass is contract mass plus exact equipped catalog mass; each catalog item contributes a uniform solid from sourced row dimensions at every declared mount, with per-item Forge-frame inertia retained in the artifact and compiled into the same MJCF inertials. Collision geometry remains contract-owned. Catalog motor Kv and battery voltage/discharge limits are retained, but exactly one bench table may drive the 101-point curve, and only when its voltage grid covers the equipped battery range and its diameter×pitch exactly matches the equipped prop; multiple applicable tables are ambiguous and fail closed. Otherwise the table, source, and rejection reasons remain in the artifact while the curve uses named inline motor/battery resistance, current-limit, prop-geometry, and `DEFAULT_CT` fallbacks. This corrects the pre-D65 HUD's unsafe edge-clamping behavior. The D12 fixture's review-gated 25.2 V/5×4.6 table is therefore bound but rejected for its 14.8–16.8 V/5×4.3 configuration; the current official [EMAX ECO II page](https://emaxmodel.com/products/emax-eco-ii-series-2207-3-6s-1700kv-1900kv-2400kv-brushless-motor-for-rc-drone-fpv-racing) was rechecked on 2026-07-17 and does not provide an applicable performance grid in its published text. The worker validates exact fields, mass closure, mount-centered COM, independently recomputed uniform-solid inertia, declared table voltage/range, unique table applicability, hashes, named fallbacks, and compiled MuJoCo mass. `forge-codesign-engine-batch`/`p9-engine-batch-evidence` 4.0.0 and `forge-codesign-training-authority/1.0.0` bind bundle/catalog-physics hashes and the applicability result per tier-2 rollout while preserving D63/D64 recovery fences. No applicable catalog thrust table, catalog collision invention, catalog-review approval, trained finalist, overnight/provider billing, live catalog, build, hardware, or field claim is created. Changing mass/inertia placement, dimension convention, table-applicability or fallback meaning, bundle/hash lineage, or these nonclaims requires another major and superseding decision. | Catalog admission alone did not make MuJoCo consume the selected physical component, while blindly edge-clamping a 4S design to a lone 6S bench point overstated thrust. A separate major preserves historical evidence and makes exact sourced mass, inertia approximation, table eligibility, remaining fallbacks, and runtime mass closure independently inspectable and fail-closed. | active |
| D66 | **File-catalog row 2.0.0 carries voltage on every bench point; v1 remains an exact single-voltage legacy read and persisted history never gains fabricated authority** (2026-07-17). Top-level `schemaVersion` is optional only for historical compatibility: missing or explicit `1.0.0` requires one table-level voltage and forbids point voltage, while current `2.0.0` forbids table voltage and requires finite positive voltage on every point. Each table has a stable ID, exact prop, positive confidence, HTTPS source, 2–10,000 points, at most 64 tables, a complete rectangular voltage×throttle grid, unique coordinates, throttle coverage from exactly 0 to 1, and nondecreasing thrust/current along throttle at every voltage. Rust file-catalog admission and Python ETL exact-match supported majors and refusals; generic thrust interpolation validates finite bounded monotonic grids. Migration 0027 preserves prior database rows as `legacy-unattributed` v1 with nullable prop/confidence/source, expands identity to `(component_id, table_id, voltage, throttle)`, and requires authority metadata for v2 rows. The checked-in EMAX row stays v1, review-gated, and D65-inapplicable; no catalog datum, training bundle, curve, marketplace approval, or physical maturity is upgraded by this format decision. Before any v2 grid drives D65 training, add a new catalog-physics/training authority major that retains the exact grid and lets Python independently recompute the curve. | A per-table voltage could not express a non-degenerate operating range, while silently merging tables or inventing metadata would corrupt source authority. A versioned explicit grid, strict old/current reads, immutable row identity, populated-predecessor migration proof, and adversarial corpus make the representation usable without confusing format capability with sourced or reviewed propulsion evidence. | active |
| D67 | **Table-driven training authority requires exact SI grid retention plus independent full-curve readback; representation, sourcing, review, and consumption remain separate facts** (2026-07-17). Catalog multirotor bundle 4.0.0 and `forge-training-catalog-physics/2.0.0` retain every selected or rejected bench point as exact voltage, throttle, thrust, and current values together with row major, table identity, prop, confidence, HTTPS source, declared ranges, and applicability reasons. Child `forge-training-catalog-curve-readback/1.0.0` freezes the 101-point throttle schedule and Rust fixed-point recipe: equipped motor count, maximum total current, nominal battery voltage, battery and motor resistance, twelve iterations, `1e-6` voltage convergence tolerance, and 0.5 minimum-voltage fraction. Python independently requires a finite complete rectangular unique grid, exact throttle endpoints, and monotonic thrust/current at every voltage; for a uniquely applicable table it bilinearly reads that retained grid, refuses if any fixed-point voltage leaves the measured axis, and reconstructs all 101 total-thrust, normalized-voltage, and normalized-current points before accepting `catalog-motor-battery-exact-grid-readback-v2`. Rejected or absent applicability requires `tableDriven=false`, null selected-table identity, the existing analytic-fallback model, and no false reconstruction claim. Coordinated `forge-codesign-training-authority/2.0.0`, engine batch/evidence 5.0.0, cache/checkpoint/resume lineage, and per-row source summaries bind exact-grid retention and verified readback while leaving search-plan 3.0.0 and prior artifacts immutable. A controlled-synthetic 10.0/16.8 V × 0/0.5/1 fixture proves acceptance, no-edge-clamp enforcement, and curve/grid tamper refusal without changing catalog data. The checked-in EMAX 25.2 V/5×4.6 v1 row remains review-gated, rejected for the 14.8–16.8 V/5×4.3 fixture, and analytic fallback remains authoritative. No sourced applicable grid, owner review, marketplace/live catalog, tier-3 policy, overnight/provider billing, build, hardware, field, or external maturity is created. Changing retained point meaning, grid validation/interpolation, fixed-point recipe/readback tolerance, table-driven label, authority hashes, or these nonclaims requires coordinated new majors and a superseding decision. | D66 made a range-spanning grid representable, but metadata-only retention could still let Rust and Python silently consume different curves. Retaining the complete measurement set and independently replaying every curve point makes the consumer boundary inspectable and tamper-evident without treating synthetic coverage or a format upgrade as real propulsion evidence. | active |

| D68 | **Managed deployments use one versioned non-secret manifest and a build-once direct-promotion ladder; topology names do not confer maturity** (2026-07-18). The supported first shape is one region with static Studio, gateway, sovereign validator, Postgres, private object storage, private lease-fenced workers, optional explicit providers, and a separate controlled-lab path. `local`, `ci`, `sandbox`, `staging`, `production`, and `controlled-lab` have machine-enforced components, gates, owners, and authority ceilings. Clean protected-main artifacts carry exact digest/SBOM/provenance and promote unchanged `protected-main -> sandbox -> staging -> production` or separately to `controlled-lab`; database migrations remain forward-only. Manifests record non-secret configuration plus versioned environment-specific secret references, never values. Managed gateway/workers verify exact manifest bytes/digest/environment/source/status/component at startup. Production cannot confer hardware or field authority, and no manifest/deploy alone proves live or field maturity. Kubernetes, active-active multi-region, and public worker ingress remain out until measured need and a superseding decision. | Scattered environment variables and a development Compose profile could otherwise be mistaken for an operable production design. One compatibility-governed authority object, strict startup binding, accountable roles, build-once promotion, secret separation, and explicit nonclaims make drift and skipped gates reviewable without fabricating a deployment. | active |
| D69 | **The first hardened runtime is a compatibility-governed single-host Docker Compose contract; CI container success remains fixture evidence until a protected digest is installed and rolled back in a managed sandbox** (2026-07-18). `forge-hardened-runtime/1.0.0` pins reviewed build, service, SBOM, and vulnerability-tool images by tag plus manifest digest. Gateway, workers, and Studio are separate multi-stage targets with exact lockfile installs, source-revision labels, numeric non-root identities, read-only roots, explicit writable mounts, dropped capabilities with only bounded stateful initialization additions, no-new-privileges, finite PID/CPU/memory limits, bounded graceful stops, and health/readiness probes. Only the TLS Studio edge publishes a host port; workers, Postgres, and TLS object storage stay on the private application network. Runtime secret values are loaded only from exact allowlisted regular non-symlink files and managed gateway/workers require their own image/artifact digest in the D68 manifest. Forward migration is a one-shot exact-gateway job. CI must build all targets, retain BuildKit provenance metadata and SPDX SBOMs, fail on reviewed fixed vulnerabilities, and exercise TLS, isolation headers, private topology, least privilege, resource bounds, readiness, graceful stop, and same-artifact restart. CI uses the `ci` authority ceiling: it proves no sandbox install, promotion, rollback, live service, production, external beta, hardware, or field maturity. A real sandbox requires immutable registry references, a protected D68 manifest, retained evidence, and install/rollback observation outside the repository. | A source-mounted development profile and mutable toolchain tags cannot be promoted safely, while a green container smoke can still overstate an ephemeral runner. One exact runtime surface makes image identity, privilege, storage, network, probe, and nonclaim drift machine-reviewable and supplies the smallest deployable substrate without introducing Kubernetes. | active |
| D70 | **Protected application images publish to repository-owned GHCR only as digest-addressed objects from one manual, protected-main, environment-gated workflow; publication is independently pulled and attested but never confers managed-runtime authority** (2026-07-18). `forge-hardened-runtime-registry/1.0.0` fixes the three lowercase proprietary image names, repository-scoped GitHub Actions credential authority, exact protected source/ref, and no-mutable-tag rule. `.github/workflows/hardened-runtime-release.yml` accepts only its exact dispatched protected `main` head, builds each D69 target once, pushes by digest, attaches BuildKit SBOM/provenance and GitHub build provenance to the registry, emits exact-registry SPDX and zero-fixed-low-or-higher scan records, then uses a separate job to pull the same manifests, hash their raw OCI bytes, verify registry-attached attestations against the exact signer/source/ref, run the D69 smoke over the pulled config digests, and emit `forge-hardened-runtime-publication/1.0.0`. Images carry `LicenseRef-ForgedTTC-Proprietary`; package visibility remains an unreviewed registry fact rather than a license grant. The evidence format permanently keeps managed environment, sandbox install, rollback, live, production, and external-beta authority false. | A mutable commit tag, workflow-local image, or successful push could otherwise be rebuilt, substituted, lose its evidence, or be mistaken for an installed service. Digest-only build-once publication plus registry-attached identity and independent pull creates a promotable artifact boundary while leaving D69's real managed-sandbox install/upgrade/rollback/corrected-forward gate intact. | active |
| D71 | **Operational telemetry is a versioned, deny-by-default event contract; trusted services generate correlation authority and identifiers never become unbounded metric labels** (2026-07-18). `forge-observability-event/1.0.0` requires UTC clock, service/package version, environment, source component/revision, opaque request/trace/span correlation, and an exact bounded attribute allowlist. Gateway request roots are server-generated UUIDv4/W3C identities; client `x-forge-request-id`, `x-request-id`, and `traceparent` values have no authority. Raw URLs/query strings, headers/cookies, bodies, prompts, error/provider text, model bytes, telemetry, personal data, presigned URLs, and secret references are outside the format. Request/user/job/provider/deployment IDs may correlate bounded logs/traces only after their owning trust boundary exists and may never be metric labels. Sink failure cannot change request authority. The first slice implements Gateway request-completion events only; worker/job/provider/Desktop propagation, metrics/traces backends, dashboards, alert routing/delivery, managed sandbox, live, and production maturity remain false. | Ad-hoc logging and client-supplied IDs can leak secrets, forge audit continuity, explode telemetry cardinality, or turn the presence of logs into a false monitoring claim. A small compatibility-governed allowlist makes the safe transport boundary reviewable before any external backend or live exercise exists. | active |
| D72 | **Job and D38-attempt observability uses a new event major, database-owned correlation, and authority-independent delivery** (2026-07-21). `forge-observability-event/2.0.0` retains the frozen v1 Gateway reader and adds exact worker-attempt started/completed events. A trusted Gateway request persists its server UUID, trace, and request span as the job parent; direct and historical jobs receive a new trace root with null request/parent rather than fabricated continuity. Every atomic D38 claim creates one database UUIDv4 attempt ID and random worker span, and the additive attempt row closes as succeeded, retry-scheduled, failed, cancelled, or expired with a stable code but never a lease token, payload, error/provider text, or result. Worker stdout contains only validated 4 KiB JSON lines; plaintext readiness stays on stderr, and sink loss cannot alter lease, retry, cancellation, materialization, or terminal authority. User export 1.7 adds owner-scoped correlation and attempt rows without secret fields. Provider/deployment/actor/Desktop correlation, telemetry transport/storage, metrics/traces, dashboards, alerts, managed sandbox, live, and production maturity remain false. Changing correlation parentage, event identity, attempt/outcome meaning, or authority independence requires another event major and a superseding decision. | D71 intentionally could not bind asynchronous work, while widening v1 would let historical readers silently misinterpret new identity and terminal semantics. A separate major plus database-generated per-claim identity makes queue continuity inspectable without turning telemetry availability into job authority or inventing unsupported provider/deployment trust. | active |
| D73 | **Deployment and provider correlation uses event major 3 and only identities already admitted by their owning authority boundary** (2026-07-21). `forge-observability-event/3.0.0` retains frozen v1/v2 read schemas. Managed Gateway and worker request/attempt events carry the bounded `deploymentId` only after D68 startup verifies active exact manifest bytes/digest, environment, protected source, clean state, component artifact, and service configuration; local/CI events require null and a managed event without that ID fails serialization. A worker completion may carry a Modal `providerCallId` only for `train.policy` after the existing D46 lease-fenced transaction persists the bounded FunctionCall ID and its deployment identity in `job_provider_calls`; started events remain null, failed pre-submit calls remain null, and recovery reuses the persisted call rather than fabricating a replacement. Provider/deployment IDs remain forbidden metric labels. No manifest bytes/digest, provider payload/output/error, token, lease, idempotency key, secret reference, actor/Desktop identity, backend, dashboard, alert, managed-sandbox, live, or production proof is added. Changing these ownership rules, broadening provider families, or coupling telemetry to request/job/provider authority requires another event major and a superseding decision. | D72 intentionally left provider/deployment correlation null. D68 and D46 now supply narrow pre-existing trust boundaries that can be projected without a new database table or external call: returning verified startup context prevents ambient environment names from becoming deployment truth, and mutating the claimed job only after durable provider-call persistence prevents pre-submit or forged call identity from reaching completion logs. | active |

D66 closes D65's representation dependency and D67 closes the downstream exact-grid/
curve-readback implementation dependency. Sourcing and owner-reviewing a voltage- and
prop-applicable grid remains separate; separate legacy sweeps are never merged,
rescaled, or inferred.

## Open decisions

Tracked as OD-items in [`TODO.md`](TODO.md) §4. The product name (D23), React face
(D16), marketplace economics (D29), alignment timing (P5-004), and binary-spawn
(D22) are resolved. Remaining decision work is asymmetric slot UX (OD-03), the WASM
user-controller sandbox (OD-04), fixed-wing priority (OD-06), plus the formal
ForgedTTC trademark scan tracked as GOV-010. Resolve a real decision by appending a
D-row here and updating the OD row; do not silently mutate earlier decisions.

## Expected near-term additions

- ~~P2: napi-rs hot-path bindings vs binary-spawn in the gateway (OD-08)~~ — resolved as D22.
- ~~P3: reference-rig SKU selection (fulfils D12).~~ — resolved in P3.
- ~~P4: pinned Anthropic model strings/limits/pricing (CLAUDE.md non-negotiable #11)~~ — resolved as D26.
- ~~P8: legal-review sign-off record (entry gate).~~ — accepted for controlled D12 lab pilots by D30.
- P11: dual-use check record remains gate-tracked; marketplace economics deferred by D29.
