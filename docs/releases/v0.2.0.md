# ForgedTTC validator v0.2.0

This candidate advances the standalone `forge-validate` command and
`@forge/validate-wasm` facade to package version 0.2.0 and ModelSpec 2.2.0. It adds
explicit equipped slot alternatives without changing validator-report, replay, or
EnvSpec major versions.

ModelSpec 2.2 adds optional-shape `slots[].equippedVariantId` with a sovereign
validator invariant: every non-empty slot must name exactly one unique variant. Only
that alternative contributes inline geometry, catalog references, mass, simulation
inputs, collider checks, lockfile requirements, or BOM rows. The WASM artifact carries
stable source JSON Pointers so Studio edits and inspection state remain attached to
the physical source part when an alternative changes the flattened part table.

Migration from 2.1 is explicit and audit-recorded:

- a slot with one alternative is deterministically equipped and records
  `equip-single-variant-slots-v2.2`;
- a slot with multiple alternatives is not guessed. Set `equippedVariantId` to the
  intended variant, then rerun `forge-validate migrate ... --to current`.

The v0.2 worker/gateway artifact boundary also introduces the independently versioned
license export manifest 1.0. Manufacturing export jobs now fail closed without
compatible per-asset ledger evidence, require attribution manifests when applicable,
and substitute no-redistribution/view-only geometry with dimensioned envelopes,
datum ports, and BOM link-outs. Live OCCT artifact proof remains a sandbox milestone.

Gateway deployments also gain the SEC-002 platform-exclusion boundary. Generation
context, synchronous/streaming generation, course generation, and model edits reject
weapons, targeting, munition, and interdiction briefs before retrieval/provider or
mutation work. Structured refusals expose `SAFETY_PROHIBITED_BRIEF` and store only
hash/bucket/version/category/rule/surface/provider/archetype/optional-owner metadata;
raw refused prompts and provider keys are neither persisted nor echoed. Failure to
write this audit row fails closed.

Gateway deployments also gain SEC-003..005 user-data and lifecycle authority APIs.
Authenticated `GET /v1/account/export` emits user-data format 1.2.0 across account
metadata, generated/models/photoscan/blob/job/replay/policy/course/platform/
classroom/telemetry/maintenance/refusal/pattern/consent records plus redacted
hold/backup exposure, with authenticated blob-download endpoints and no OAuth/
session/verification/provider secrets. Exact-confirmation `DELETE /v1/account` uses
a serializable owner lock, explicit row purges, S3-compatible payload deletion before
commit, and transaction-scoped user/object authority locks. Deletion receipt 2.0.0
proves primary Postgres/object removal and records pseudonymous restore-suppression
tombstones; it does not claim physical provider-backup deletion.

Consent ledger 1.0.0 adds append-only current-notice grants and withdrawals for
photoscan processing per object, telemetry sharing and training reuse per log,
pattern contribution per model, and leaderboard publication per account. Active
owned-subject consent is rechecked under the same serializable owner lock as each
action, including generic/direct photoscan and training job entry points. Withdrawal
cancels affected pending work or removes primary sharing/retrieval/publication
eligibility; it does not claim provider recall or backup erasure.

Data-lifecycle 1.0.0 adds six versioned retention classes, time-bounded append-only
legal holds, exact backup subject manifests, a fail-closed restore gate, bounded
backup-deletion retries with stale-claim recovery, and dry-run-first primary sweeps.
`GET /v1/data-lifecycle/policy` and authenticated `GET /v1/account/lifecycle` expose
only bounded state. This is contract/local-Postgres proof; real encrypted backup
automation, provider deletion evidence, sandbox restores, and measured RPO/RTO remain
the separate OPS-005 production gate.

Compatibility: validator report 1.x, replay 1.x plus the deprecated `replay.v1`
read alias, EnvSpec 1.x, license export manifest 1.x, user-data export 1.x, consent
ledger 1.x, account-deletion receipt 2.x, and data-lifecycle 1.x. See
[`../COMPATIBILITY.md`](../COMPATIBILITY.md).

This release does not include FORGE Desktop, live providers, hosted operations, or
field-proven hardware claims. Hosted gateway deployments must apply the additive
`0015_generation_refusals.sql` migration; it has no backfill or object-storage impact.
SEC-004 also applies additive `0016_user_consent_events.sql`, which adds consent
history and owner/model provenance for opt-in patterns. SEC-003 adds no database
migration. SEC-005 adds `0017_data_lifecycle.sql` and
`0018_authority_event_sequences.sql` for retention policies, hold/backup/tombstone/
restore/audit records, and monotonic consent/hold chronology;
`0019_authority_sequence_backfill.sql` derives pre-existing chronology from causal
links instead of heap order. Application rollback
removes its routes but does not restore primary rows or objects already deleted;
retain lifecycle records so restore suppression remains effective, and roll forward
with a reviewed migration instead of dropping authority history.
On application rollback, retain the refusal audit table unless an explicit retention/
privacy decision authorizes an exported-and-backed-up purge. Standalone validator
rollback restores the v0.1.0 binary/package; documents saved with multiple ModelSpec
2.2 alternatives need an explicit 2.1-compatible conversion and must never silently
fall back to array order. Verify `SHA256SUMS` before use.
