# RISK REGISTER

Source: plan §20 (v3.0, binding), extended with watch triggers — the observable
signal that says a mitigation is failing and the risk needs active management. Review
at every phase close; note material changes in the changelog.

| # | Risk | L | I | Mitigation | Watch trigger |
|---|---|---|---|---|---|
| R1 | Sim-to-real gap (especially legged) | High | High | archetype gradient (quads/rovers first); estimator-in-sim (D8); randomization + system ID as product rituals; ladder gating; honest scorecards | ghost divergence stays large after system-ID fit on the reference quad (P8) |
| R2 | LLM emits plausible-but-invalid geometry | High | Med | schema-constrained output; bounded self-repair on machine diagnostics; pattern grounding; draft fallback (D14); Brief-25 as CI | Brief-25 admission < 20/25 or repair iterations trending up |
| R3 | CAD/license entanglement | Med | High | license ledger from day one; export matrix (D10); derived-LOD serving; link-out fallback; legal review before marketplace | any ingested asset found without a ledger entry; takedown request received |
| R4 | Open-core commoditization (fork + clone) | Med | Med | the moat is the data, not the code (D2): catalog + ledger, pattern corpus, provenance graph, community courses/skills | a fork ships a competing catalog of comparable quality |
| R5 | Generation-eval drift (model bumps silently degrade quality) | Med | Med | Brief-25 re-runs on every prompt/schema/pattern/model change with tracked metrics | dashboard regression after any model-version bump |
| R6 | GPU cost creep (photoscan/training/co-design) | Med | Med | burst-only GPUs; permanent caching; Batch API ETL; multi-fidelity ladder; MJX only when CPU saturates (P7-010) | monthly GPU spend exceeding credit revenue two months running |
| R7 | Print/build liability | Low–Med | High | DfM diagnostics framed as checks not guarantees; service/user own outcomes; ToS review gates P8; supervisor disclaimers | any incident report involving a printed structural part |
| R8 | Scope gravity — it wants to be five products | High | High | the loop is the spine; the success ladder (plan §1.3) names the rungs; every phase has exit criteria; platform phases deliberately last | work started on a phase whose predecessor has unmet exit criteria |
| R9 | Two-engine physics divergence | Med | Med | one canonical scenario-contract source; contract-derived MJCF with explicit radian units; exact MuJoCo pin; matched timestep/substeps; real four-scenario execution in the required worker check; registered keyless baseline and review-gated re-pins; training side canonical (D20) | tolerance, source/provider, unit, timestep, or substep failure after an engine/exporter bump |
| R10 | Solo-builder bus factor | High | Med | boring choices everywhere but one — and the one (the Rust core) is the most documentable, testable part; validator-enforced invariants; this documentation system + changelog discipline | sessions ending without changelog entries; docs drifting from code |
| R11 | Browser API churn (WebSerial/WebGPU) | Low | Med | WebGL2 baseline on isolated desktop Chromium; Firefox/WebKit use a dependency-light core-baked Canvas2D schematic without loading WebGL chunks; QA-003 runs the locked three-engine matrix; **FORGE Desktop carries the bridge past the browser (D15)** | an engine release breaks WASM, local-session fallback, schematic drawing, viewer interaction, isolation/SAB behavior, asset isolation, or the declared tier |
| R12 | **Rust port costs more than estimated** (solo, TS-native builder) | Med–High | Med | core is math-not-frameworks Rust; harness-as-oracle makes "done" objective; per-crate landing order (contract → motion → geometry → sim → validate); AI pair; sanctioned fallback: ship a lagging crate as TS behind the frozen boundary (with a DECISIONS entry) | a crate not green against its oracle after its scheduled slice of the P1 window |
| R13 | **Cross-target float divergence or casual re-pinning breaks D17** | Low–Med | Med | no fast-math anywhere in core; golden-number suite in CI on every core change; machine registry, immutable prototype oracle, append-only update records, and declared ULP-tolerance degradation per offending platform | any golden-number failure or registered schema/render/physics drift that cannot be traced to a reviewed source-of-truth change |
| R14 | **Living docs drift from executable truth** | High | High | evidence-first authority order; dated PROJECT-STATE; stable TODO IDs; reconcile status in every behavior change | CI/test evidence contradicts a checked roadmap/TODO claim |
| R15 | **Fixture breadth is mistaken for product maturity** | High | High | contract/fixture/sandbox/live/field labels; QA-010's versioned external scripts require exact revision/environment, independent roles, authority, evidence hashes, measurements, signoffs, limitations, and preserved pass/fail/stop outcomes; no phase close from route/table/template or structural-manifest presence | public/internal claims use fixture counts, generated runbooks, self-attestation, or unresolved evidence references as live/field proof |
| R16 | **Toolchain and workflow drift silently break gates** | High | High | pin/version policy; local verify command matching CI; scheduled gate ownership; artifact diagnostics | stable toolchain/action update turns repeated CI/nightly red |
| R17 | **Supply-chain or unprotected-main compromise** | Med | High | protected ruleset; immutable Action pins; dependency/secret/code scanning; SBOM/attestation; least workflow permissions; normalize and verify native executable modes after Actions artifact transfer | direct main push, unreviewed workflow change, non-executable downloaded CLI, or unresolved advisory beyond SLA |
| R18 | **Privacy/deletion promise exceeds implementation** | Med | High | D33 export/primary deletion, D34 purpose/subject consent, and D35 retention/hold/tombstone/restore suppression are contract/fixture proven; OPS-005 must automate encrypted backup coverage, provider deletion evidence, isolated restores, alerts, and measured RPO/RTO before any live claim | primary deletion leaves residue, a storage failure commits DB deletion, secrets enter an export, a stale/withdrawn grant authorizes work, a hold races deletion, a deleted subject reappears after restore, or a backup misses its deletion deadline |
| R19 | **Provider failure or cost destroys trust/economics** | Med | High | D36 gives native ETL an exact endpoint/model/version, finite request/response/tool budgets, local validation, and provenance; P11-005 now gives vendor refresh one owner-scoped/request-bound idempotent local queue path, command-required execution, sanitized holds, bounded normalization, and transactional revalidation while retiring the gateway live-HTTP bypass; credentialed sandboxes must still add egress allowlists, quotas, circuit breakers, cancellation/refunds, cost telemetry, current terms, and recovery | repeated partial artifacts/offers, a queued vendor job silently using fixture truth, a client key crossing tenants or changing request meaning, invalid purchase truth committing with job success, runaway spend, unexplained credit loss, or a provider candidate bypassing review |
| R20 | **Hosted Intel release latency makes artifact proof unreliable or prohibitively slow** | Low | Med | supported macOS 26 Intel plus measured thin LTO, 60-minute ceilings, executable-mode normalization, macOS 15 rollback through August 2027, and unchanged smoke/SBOM/attestation gates are proven by protected-main run `29241883791` and tag run `29244972303` | any native release job exceeds 30 minutes, times out, or loses executable/archive determinism |
| R21 | **Application threat controls are mistaken for production perimeter proof** | Med | High | SEC-006 and D36 bound auth, origins, HTTP BYO and ETL service keys, JSON, outbound HTTPS, provider envelopes, workers, objects, rate classes, errors, and release archives; `THREAT-MODEL.md` still requires connection-time egress, shared quotas, separate key/log drills, workload isolation, and incident evidence before live claims | a private/metadata connection succeeds, a key appears in request JSON/persistence/logs/errors, one replica bypasses quota, an unscanned upload is consumed, or operations marks the service live without the deployment checklist |
| R22 | **Visual success hides an accessibility or viewer-compatibility regression** | Med | High | semantic HTML first; visible focus and skip navigation; keyboard viewer/configurator journey; live announcements; AA contrast; critical target, responsive, and reduced-motion assertions; full-WebGL versus Canvas2D renderer/asset/draw assertions in the locked Chromium/Firefox/WebKit production-bundle matrix; qualified user/device review remains an explicit external gate | a control loses its name, keyboard/focus path, announcement, AA contrast, target size, narrow containment, reduced-motion behavior, the fallback goes blank/loads WebGL, or the UI claims more than the tested engine/device tier |
| R23 | **Migration history diverges or a deploy leaves partial persisted state** | Med | High | D37 forward-only prefix support; advisory-lock serialization; per-file SQL plus ledger transaction; checksum/gap/missing-source refusal; clean and every populated predecessor acceptance; injected rollback/roll-forward and concurrency proof; predeploy backup and application-rollback runbook | two runners race, a failed migration leaves DDL/data or a ledger row, a supported prefix loses data, an unchanged rerun mutates history, or an operator edits checksums/ledger rows to bypass refusal |
| R24 | **At-least-once compute or partial uploads corrupt product truth** | Med | High | D38 opaque expiring attempt fences, persisted timeout authority, bounded retry taxonomy/ceiling, cancellation-first compare-and-set completion, transactional materialization, checksum-bound staged uploads, server-side exact metadata inspection, and isolated-Postgres crash/duplicate/outage/rate/partial fault acceptance | a stale or cancelled attempt commits, one job materializes twice, retry loops exceed policy, a staged/mismatched object downloads or authorizes photoscan, or production partitions/backlogs cannot be reconciled |
| R25 | **Malformed cross-boundary data bypasses physical, provenance, export, or hardware authority** | Med | High | QA-007 registers one exact eight-surface/89-case adversarial family with Rust/Python consumers and random no-panic coverage; finite SI/time/confidence rules, import graph checks, bounded JSON, HTTPS citations, D10 policy agreement, safe command tokens, unique telemetry time, and strict supervisor vectors/limits fail closed | a corpus file/outcome disappears without review, a NaN/Infinity verifies or publishes, malformed import text panics/defaults to plausible geometry, a command newline reaches output, duplicate telemetry enters replay, or malformed supervisor state permits policy |
| R26 | **Policy object/metadata substitution or orphaned uploads create false training authority or unbounded residue** | Med | High | P7-011 binds one job, admitted model revision, exportable scorecard, tensor header, lineage, byte length, SHA-256, owner content-addressed object, and complete storage row; D38 is checked before upload and in the serializable one-winner materialization; gateway and Studio independently verify before execution. Protected PR #68/`9131289` artifact `8340587390` proves one winner, stale/substitution refusal, cancellation without database authority, exact readback, and owner-scoped browser execution at controlled S3-compatible sandbox maturity; OPS-006 owns bounded orphan inventory/deletion and production storage operations | retained bytes differ from any metadata layer, one job creates multiple policies, a cross-owner policy is readable, inline bytes enter persisted output/export, a cancelled upload gains authority, or unreferenced content-addressed objects grow without inventory/deletion evidence |

L = likelihood, I = impact.

**Adding a risk:** append a row (next free R-number), set L/I, name a real mitigation
and an observable trigger, and reference it from the changelog entry that introduced
the concern.
